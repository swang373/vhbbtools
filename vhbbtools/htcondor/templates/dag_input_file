# Automatically generated on {{ timestamp.strftime('%a %b %d %H:%M:%S %Z %Y') }}
{% for i in range(number_of_jobs) %}
JOB job{{ i }} node
VARS job{{ i }} job="job{{ i }}"
RETRY job{{ i }} 3
{% endfor %}

